os: osx
language: node_js

addons:
  homebrew:
    packages:
      - hugo
    update: true

script:
  - hugo

deploy:
  provider: pages
  target-branch: master
  local-dir: public
  repo: golubkov-p.github.io
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: master
